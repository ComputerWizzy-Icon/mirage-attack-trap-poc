# --- Network Configuration ---
ethereum_rpc = "https://0xrpc.io/hoodi"
drosera_rpc = "https://relay.hoodi.drosera.io"
eth_chain_id = 560048
drosera_address = "0x91cB447BaFc6e0EA0F4Fe056F5a9b1F14bb06e5D"

# --- Trap Metadata ---
name = "mirage-attack-trap"
description = "Detects Mirage attacks by analyzing cross-chain price deltas, liquidity shifts, and malformed packets."
version = "1.0.0"

[metadata]
author = "Sekani Chief JayCool BFF"
risk_class = "Cross-chain Mirage Attack"
category = "anomaly-detection"
use_case = "Detects desynced market states and triggers on suspicious packet mismatches."

# --- Trap Configuration ---
[traps]

[traps.mirage_attack]
path = "out/MirageTrap.sol/MirageTrap.json"
response_contract = "0x0000000000000000000000000000000000000000"   # (Optional) If you later add a Responder
response_function = ""                                                   # leave blank if not using a responder yet
block_sample_size = 5
cooldown_period_blocks = 0
private_trap = false                 # set to true if you want only YOUR wallet to trigger it
whitelist = ["0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266"]  # Anvil wallet or deployer
